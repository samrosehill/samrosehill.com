---
import { getCollection } from 'astro:content';

export async function getStaticPaths() {
  const cases = await getCollection('cases');
  return cases.map((c) => ({
    params: { id: c.id },
    props: { entry: c },
  }));
}

return Astro.redirect('/cases', 301);
---
